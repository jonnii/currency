import { convert } from '../../../helpers/convert';
import { module, test } from 'qunit';

module('Unit | Helper | convert');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = convert(42);
  assert.ok(result);
});
